name: Build Visora Flutter App

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout the repo
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Flutter SDK
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.3'
          channel: 'stable'

      # 3️⃣ Enable Platforms
      - name: Enable Platforms
        run: |
          flutter config --enable-web
          flutter config --enable-android

      # 4️⃣ Clean Project & Gradle Cache
      - name: Clean Project & Gradle Cache
        run: |
          flutter clean
          rm -rf ~/.gradle/caches/
          rm -rf ~/.pub-cache/

      # 5️⃣ Install Dependencies
      - name: Install Dependencies
        run: flutter pub get

      # 6️⃣ Check Flutter doctor
      - name: Doctor check
        run: flutter doctor -v

      # 7️⃣ Ensure Android Embedding V2 (safe fallback)
      - name: Ensure Android Embedding V2
        run: |
          find android/app/src/main -type f -name "*.kt" -exec sed -i 's/FlutterActivity/FlutterActivity/g' {} \;

      # 8️⃣ Build Android APK
      - name: Build Android APK
        run: flutter build apk --release --flavor standard --verbose

      # 9️⃣ Build Web Version
      - name: Build Web Version
        run: flutter build web --release

      # 🔟 Upload Android APK Artifact
      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: visora-release-apk
          path: build/app/outputs/flutter-apk/app-standard-release.apk

      # 1️⃣1️⃣ Upload Web Build Artifact
      - name: Upload Web Build
        uses: actions/upload-artifact@v4
        with:
          name: visora-web-build
          path: build/web

      # 1️⃣2️⃣ Auto Create GitHub Release (Direct APK Download Link)
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        if: success()
        with:
          files: build/app/outputs/flutter-apk/app-standard-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
